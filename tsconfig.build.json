// Emit config: same settings as the typecheck config, minus the tests —
// they are typechecked but must not ship in dist/.
{
  "extends": "./tsconfig.json",
  "exclude": ["src/**/*.test.ts", "src/testing/**"]
}
